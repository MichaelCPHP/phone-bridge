[general]
context=default
bindaddr=0.0.0.0
bindport=5060
transport=udp

[android-phone]
type=friend
secret=phonebridge123
host=dynamic
context=from-phone
qualify=yes

; ⚠️  SECURITY: Change 'secret' values before any network-exposed deployment
; These are placeholder credentials for local LAN testing only.
