flask>=3.0.0
requests>=2.31.0
httpx>=0.27.0
websockets>=12.0

# AI — OpenClaw gateway (OpenAI-compatible) or Ollama (same client)
openai>=1.0.0

# STT — whisper-cpp (primary, Metal-accelerated) via stt_voicebox.py
# Install: brew install whisper-cpp
# Model:   whisper-cpp --download-model tiny.en

# TTS — Kokoro 82M (local, no API key, no 'say', Docker compatible)
kokoro>=0.9.0
soundfile>=0.12.0
resampy>=0.4.0
